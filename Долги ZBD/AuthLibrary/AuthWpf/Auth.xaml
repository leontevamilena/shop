<Window x:Class="AuthWpf.Auth"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AuthWpf"
        mc:Ignorable="d"
        Title="Auth" Height="450" Width="800" Loaded="Window_Loaded" Background="LightBlue">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="LightPink"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="White"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>
    
    <StackPanel HorizontalAlignment="Center">
        <Button Content="Выйти" x:Name="ExitButton" Click="ExitButton_Click" Width="120" Height="50"/>
        <TextBlock x:Name="LoginTextBlock"></TextBlock>
        <Image Source="\Images\cat.jpg" Width="300"/>
    </StackPanel>
</Window>
